export class Employee {
  /**
   * Firebase Generate key of type string
   * in databse to identify each data
   */
  $key: string;
  name: string;
  position: string;
  office: string;
  salary: number;
}
